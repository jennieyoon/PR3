import java.util.Scanner;
 
public class ArrayPlay 
{
   public static void main(String[] args) {
     Scanner scnr = new Scanner(System.in);
     String userInput = "";
     int size = 0;
      
     System.out.print("> ");
      
     /*while (userInput.equals("new")){ 
       if (//0 elements){
            
           //create empty IntArray11
       }
       if else(//inputs int size){
           //create an array with int size and store. respond "ok" if it is an int. otherwise error.
       }
      }
      break
      while(userInput.equals("print"){
         //print System.out.println(Arrays.toString(smarsArray));
         //if they enter in "index", only print that index  smarsArray[
         //set nElem = scnr.nextLine(); to access array smarsArray[nElem];
       }
             
        
     }*/
      
   }
}
